   # PostgreSQL Settings
   POSTGRES_USER=postgres
   POSTGRES_PASSWORD=postgres
   POSTGRES_DB=reventa
   POSTGRES_SERVER=postgres  # Use 'postgres' for Docker, 'localhost' for local development
   POSTGRES_PORT=5432
   
   # API Settings
   SECRET_KEY=your_secret_key_here_replace_with_a_strong_random_string
   ALGORITHM=HS256
   ACCESS_TOKEN_EXPIRE_MINUTES=30
   
   # Database URL for SQLAlchemy (constructed from above values)
   # Uncomment and modify if you need a custom format
   # DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_SERVER}:${POSTGRES_PORT}/${POSTGRES_DB}